FROM aursu/pearbuild:7-php7-7.1

RUN yum -y install \
        pcre-devel \
    && yum clean all && rm -rf /var/cache/yum

RUN yum -y --disablerepo=* --enablerepo=custom install \
        php7-pecl-apcu-devel \
    && yum clean all && rm -rf /var/cache/yum
