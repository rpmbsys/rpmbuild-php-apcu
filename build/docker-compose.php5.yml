version: "3.5"
services:
  centos7apcuphp5build:
    networks:
      - buildnet
    build:
      context: .
      args:
        image: php-5.6
      network: buildnet
    image: aursu/peclbuild:7-php-apcu-5.6

networks:
  buildnet:
    external: true
    name: buildnet
