version: "3.5"
services:
  centos7apcuphp73build:
    networks:
      - buildnet
    build:
      network: buildnet
      context: .
    image: aursu/peclbuild:7-php-apcu-7.3
  centos8apcuphp73build:
    networks:
      - buildnet
    build:
      network: buildnet
      context: .
      args:
        centos: 8
    image: aursu/peclbuild:8-php-apcu-7.3

networks:
  buildnet:
    external: true
    name: buildnet
